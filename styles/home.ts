import { css } from "@emotion/react";

export const styles = {
  sampleButton: css`
    width: auto;
    background: coral;
    color: white;
    border: solid 1px rgba(0, 0, 0, 0.1);
    border-radius: 4px;
    padding: 8px 12px;
    cursor: pointer;
  `,
};
